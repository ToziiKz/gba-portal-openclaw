# 2026-02-12

## 🚀 GBA Portal Launch
- **Server Started**: Next.js 16.1.6 running on port `3000` (PID `oceanic-dune` / `230998`).
- **Access**:
    - Local: `http://localhost:3000`
    - Public: `http://76.13.33.246:3000` (listening on `0.0.0.0`)

## 🎨 Visual Improvements (Dashboard)
- **Goal**: Remove "blue effect" and improve global dark theme consistency.
- **Changes**:
    - `globals.css`: Neutralized `card-shell` and `premium-card` styles (removed cyan/blue gradients, switched to subtle white/gray glassmorphism).
    - `DashboardHero.tsx`: Removed bright radial gradients and white backgrounds.
    - `DashboardSpotlight.tsx`: Switched modal backdrop from white/70 to black/60.
    - `DashboardTopbar.tsx`: Switched background from white/80 to `var(--ui-surface)`.
- **Outcome**: Dashboard now respects the global dark theme (`#020202`) without jarring bright/blue blocks.
